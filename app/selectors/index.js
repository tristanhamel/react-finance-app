import * as mortgage from './mortgage.selectors';
import * as localization from './localize.selector';
import * as compound from './compound.selectors';

export default {
  mortgage,
  localization,
  compound
};
